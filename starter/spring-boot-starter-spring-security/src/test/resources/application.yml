healthan:
  authentication:
    remember-me:
      always: true
    audit:
      type: memory
    captcha-verification:
      verify-urls:
        - "/login/**"
    permit-uri-ant-matchers:
      - "/prepare/**"
      - "/login/**"
      - "/logout/**"
      - "/actuator/**"


management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  redis:
    host: 17.0.0.14
    port: 6379
    password: 33DEfda34VD
    database: 11